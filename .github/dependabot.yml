version: 2
updates:
  # Keep the pinned GitHub Actions (actions/checkout, actions/cache) current.
  # No package managers beyond that: the feed ships plain shell/node tooling.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5